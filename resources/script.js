const browse = document.getElementById('browse');
const subpage = document.getElementById('subpage');

showSubpageButton.addEventListener('click', function () {
    subpage.classList.toggle('hidden');
});
